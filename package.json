{
  "name": "docsy-test",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "hugo --minify",
    "serve": "hugo server -D --disableFastRender",
    "prepare": "hugo mod get github.com/google/docsy@v0.15.0 && hugo mod npm pack && npm install --ignore-scripts"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.21",
    "hugo-extended": "0.157.0",
    "postcss-cli": "^11.0.1"
  },
  "workspaces": [
    "packages/hugoautogen"
  ]
}
